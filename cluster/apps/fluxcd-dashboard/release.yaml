apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: fluxcd-dashboard
  namespace: weaver
spec:
  releaseName: fluxcd-dashboard
  chart:
    spec:
      chart: weave-gitops
      sourceRef:
        kind: HelmRepository
        name: weave
        namespace: flux-system
      version: 4.0.36
  interval: 5m
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
  values:
    service:
      type: NodePort
    adminUser:
      create: true
      passwordHash: $2a$10$2LBXbImXzotydLOLDG1lJO..jR5jYZAovRHyNVHUktKbPKjHtIxuu
      username: admininfra
    image:
      repository: ghcr.io/weaveworks/wego-app
      tag: v0.36.0
    ingress:
      enabled: no
      annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
        cert-manager.io/cluster-issuer: letsencrypt-terracloud
        cert-manager.io/common-name: fluxcd.terracloud.lifoto.co
      hosts:
        - host: fluxcd.terracloud.lifoto.co
          paths:
            - path: /
              pathType: ImplementationSpecific
      tls:
        - secretName: fluxcd-dashboard-tls
          hosts:
            - fluxcd.terracloud.lifoto.co