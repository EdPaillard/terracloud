apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: terracloud-ingress
  namespace: terracloud
  labels:
    app: terracloud-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: https
    cert-manager.io/cluster-issuer: letsencrypt-terracloud
    cert-manager.io/common-name: terracloud-lil.local
spec:
  rules:
  - host: terracloud-lil.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: terracloud-service
            port: 
              number: 80
  tls:
  - hosts:
    - terracloud-lil.local
    secretName: terracloud-lil-tls